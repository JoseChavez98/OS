#include <stdio.h>
#include <sys/time.h>

void pointlessFunction(){
    printf("El costo de llamar a una función mínima en C /C++: ")
}

int main(){

    struct timeval start;
    struct timeval finish;
    long compTime;
    double Time;

    gettimeofday(&start, 0);

    return 0;
}